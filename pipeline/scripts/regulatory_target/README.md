# Regulatory Targets

| Stage | Script | Purpose |
| --- | --- | --- |
| 07 | `07_define_regulatory_targets.py` | Intersect promoter-bound genes with DEGs and render regulatory target figures. |

Run Stage 07 with `--publication-figures` to render text-free versions of every
panel in `regulatory_work/visuals/publication_figures`. They use the `_noTexts` suffix.
